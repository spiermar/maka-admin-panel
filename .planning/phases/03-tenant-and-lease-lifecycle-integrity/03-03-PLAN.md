---
phase: 03-tenant-and-lease-lifecycle-integrity
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - app/(dashboard)/rentals/tenants/page.tsx
  - app/(dashboard)/rentals/tenants/new/page.tsx
  - app/(dashboard)/rentals/tenants/[id]/page.tsx
  - app/(dashboard)/rentals/tenants/[id]/client.tsx
  - app/(dashboard)/rentals/tenants/[id]/edit/page.tsx
autonomous: true
requirements:
  - LEASE-01

must_haves:
  truths:
    - "User can view list of all tenants"
    - "User can create new tenant with name, phone, email"
    - "User can view tenant detail with linked lease info"
    - "User can edit tenant contact information"
  artifacts:
    - path: "app/(dashboard)/rentals/tenants/page.tsx"
      provides: "Tenant list view with search"
    - path: "app/(dashboard)/rentals/tenants/new/page.tsx"
      provides: "Create tenant form"
    - path: "app/(dashboard)/rentals/tenants/[id]/page.tsx"
      provides: "Tenant detail page"
    - path: "app/(dashboard)/rentals/tenants/[id]/edit/page.tsx"
      provides: "Edit tenant form"
---

<objective>
Create tenant UI pages: list, create, detail, and edit.

Purpose: Allow landlords to manage tenant records through the UI.
Output: Functional tenant management pages
</objective>

<execution_context>
@/Users/mspier/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tenant-and-lease-lifecycle-integrity/03-CONTEXT.md
@.planning/phases/03-tenant-and-lease-lifecycle-integrity/03-RESEARCH.md
@lib/db/rentals-tenants.ts
@lib/actions/rentals.ts
@app/(dashboard)/rentals/units/[id]/page.tsx

<interfaces>
<!-- Key functions from prior plans -->
From lib/db/rentals-tenants.ts:
```typescript
export async function createTenant(data: CreateTenantInput): Promise<Tenant>
export async function getTenantById(id: number): Promise<Tenant | null>
export async function getAllTenants(search?: string): Promise<Tenant[]>
export async function updateTenant(id: number, data: Partial<CreateTenantInput>): Promise<Tenant>
```

From lib/actions/rentals.ts:
```typescript
export async function createTenantAction(formData: FormData): Promise<ActionResult>
export async function updateTenantAction(formData: FormData): Promise<ActionResult>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tenant list and create pages</name>
  <files>app/(dashboard)/rentals/tenants/page.tsx, app/(dashboard)/rentals/tenants/new/page.tsx</files>
  <action>
Create app/(dashboard)/rentals/tenants/ directory with:

**page.tsx (Tenant list):**
- Server component that calls getAllTenants()
- Display tenants in a table with columns: Name, Phone, Email, Created At
- Add search input that filters by name/email
- Add "New Tenant" button linking to /rentals/tenants/new
- Handle empty state ("No tenants yet. Add your first tenant.")
- Follow pattern from app/(dashboard)/rentals/units/page.tsx

**new/page.tsx (Create tenant):**
- Full-page form with fields: Name (required), Phone (optional), Email (optional)
- Use form validation from createTenantSchema
- Submit via createTenantAction server action
- On success: redirect to tenant detail page
- On error: show validation errors inline
- Cancel button returns to tenant list
- Follow pattern from app/(dashboard)/rentals/units/new/page.tsx
  </action>
  <verify>
<automated>ls -la app/\(dashboard\)/rentals/tenants/page.tsx app/\(dashboard\)/rentals/tenants/new/page.tsx</automated>
  </verify>
  <done>Tenant list shows all tenants, create page submits successfully</done>
</task>

<task type="auto">
  <name>Task 2: Create tenant detail and edit pages</name>
<files>app/(dashboard)/rentals/tenants/[id]/page.tsx, app/(dashboard)/rentals/tenants/[id]/client.tsx, app/(dashboard)/rentals/tenants/[id]/edit/page.tsx</files>
<action>
Create app/(dashboard)/rentals/tenants/[id]/ directory with:

**page.tsx (Tenant detail server component):**
- Fetch tenant by ID using getTenantById
- Also fetch any active lease for this tenant (via getAllLeases with tenant_id filter)
- Pass data to client component
- Show 404 if tenant not found

**client.tsx (Tenant detail client component):**
- Display tenant info: name, phone, email
- Display linked lease info if exists: unit number, status, dates, rent
- Show "Edit" button linking to /rentals/tenants/[id]/edit
- Show empty state if no lease linked ("No active lease")

**edit/page.tsx (Edit tenant):**
- Pre-populate form with existing tenant data
- Fields: Name, Phone, Email (all editable)
- Submit via updateTenantAction
- On success: redirect back to tenant detail
- On error: show validation errors
- Cancel button returns to detail page

Follow patterns from app/(dashboard)/rentals/units/[id]/ page structure.
  </action>
  <verify>
<automated>ls app/\(dashboard\)/rentals/tenants/\[id\]/page.tsx app/\(dashboard\)/rentals/tenants/\[id\]/client.tsx app/\(dashboard\)/rentals/tenants/\[id\]/edit/page.tsx</automated>
  </verify>
  <done>Tenant detail shows info + lease, edit page updates successfully</done>
</task>

</tasks>

<verification>
- Tenant list shows all tenants with search
- Create tenant works with validation
- Detail page shows tenant info and linked lease
- Edit page updates tenant successfully
</verification>

<success_criteria>
- [ ] /rentals/tenants shows tenant list
- [ ] /rentals/tenants/new creates tenant
- [ ] /rentals/tenants/[id] shows tenant detail with lease info
- [ ] /rentals/tenants/[id]/edit updates tenant
</success_criteria>

<output>
After completion, create `.planning/phases/03-tenant-and-lease-lifecycle-integrity/03-03-SUMMARY.md`
</output>