---
phase: 03-tenant-and-lease-lifecycle-integrity
plan: 05
type: execute
wave: 4
depends_on:
  - 03-03
  - 03-04
files_modified:
  - components/layout/navigation.tsx
  - app/(dashboard)/rentals/units/[id]/client.tsx
autonomous: true
requirements:
  - LEASE-01

must_haves:
  truths:
    - "Navigation shows Tenants and Leases menu items under Rentals"
    - "Unit detail page shows linked tenant and active lease info"
  artifacts:
    - path: "components/layout/navigation.tsx"
      provides: "Navigation with Tenants and Leases items"
      contains: "rentals/tenants"
      contains: "rentals/leases"
    - path: "app/(dashboard)/rentals/units/[id]/client.tsx"
      provides: "Unit detail with linked tenant/lease section"
      shows: "tenant name, lease status, lease dates"
---

<objective>
Integrate tenant and lease pages into navigation and unit detail.

Purpose: Connect new features into existing app structure for seamless UX.
Output: Updated navigation and unit detail showing tenant/lease info
</objective>

<execution_context>
@/Users/mspier/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tenant-and-lease-lifecycle-integrity/03-CONTEXT.md
@app/(dashboard)/rentals/units/[id]/client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update navigation to include Tenants and Leases</name>
<files>components/layout/navigation.tsx</files>
<action>
Find the rentals navigation section in navigation.tsx and add:

```
- Tenants (/rentals/tenants)
- Leases (/rentals/leases)
```

Existing structure probably looks like:
```
{
  title: 'Rentals',
  items: [
    { title: 'Properties', href: '/rentals/properties', icon: Building2 },
    { title: 'Units', href: '/rentals/units', icon: Home },
    // Add these:
    { title: 'Tenants', href: '/rentals/tenants', icon: Users },
    { title: 'Leases', href: '/rentals/leases', icon: FileText },
  ],
}
```

Find existing icon imports (Building2, Home) and add icon imports for Users and FileText from lucide-react.
  </action>
  <verify>
<automated>grep -l "rentals/tenants" components/layout/navigation.tsx && grep -l "rentals/leases" components/layout/navigation.tsx</automated>
  </verify>
  <done>Navigation shows Tenants and Leases under Rentals section</done>
</task>

<task type="auto">
  <name>Task 2: Update unit detail to show linked tenant and lease</name>
<files>app/(dashboard)/rentals/units/[id]/client.tsx</files>
<action>
Update the unit detail client component to show tenant/lease information:

**Add imports:**
- getTenantByUnitId from lib/db/rentals-tenants
- getAllLeases from lib/db/rentals-leases

**Fetch data:**
- In page.tsx, also fetch:
  - tenant = await getTenantByUnitId(unitId)
  - leases = await getAllLeases({ unitId })
- Pass tenant and leases to client component

**In client component (client.tsx):**
Add a new section below the unit info:

```
## Tenant & Lease

{if tenant exists}
  - Tenant: {tenant.name}
  - {if active lease exists}
    - Lease Status: {lease.status} (badge)
    - Start Date: {lease.start_date}
    - End Date: {lease.end_date}
    - Monthly Rent: ${lease.monthly_rent}
    - View Lease button → /rentals/leases/{lease.id}
  {else if no active lease}
    - "No active lease" indicator
{else}
  - "No tenant assigned" with Add Tenant link → /rentals/tenants/new
```

Show empty state when no tenant/lease. Use existing card/section styling from the component.

This fulfills CONTEXT.md requirement: "Update unit detail page: show linked tenant/lease info"
  </action>
  <verify>