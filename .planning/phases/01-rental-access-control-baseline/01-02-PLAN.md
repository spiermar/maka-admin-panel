---
phase: 01-rental-access-control-baseline
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - e2e/10-rentals-auth.spec.ts
autonomous: true
gap_closure: true
requirements:
  - VIS-03
user_setup: []
must_haves:
  truths:
    - Rental auth e2e coverage remains explicit for both unauthenticated redirect and authenticated access.
    - The previously failed artifact contract is closed by making `e2e/10-rentals-auth.spec.ts` substantive and maintainable.
  artifacts:
    - path: e2e/10-rentals-auth.spec.ts
      provides: Meaningful browser-level auth assertions for rental route access control
      min_lines: 30
      contains: /rentals
  key_links:
    - from: e2e/10-rentals-auth.spec.ts
      to: app/(dashboard)/rentals/page.tsx
      via: Playwright navigation and URL assertions for auth outcomes
      pattern: /rentals
---

<objective>
Close the single verification gap in Phase 1 by fixing the only failed artifact contract.

Purpose: Restore full verification compliance for VIS-03 without expanding scope beyond already-implemented auth baseline behavior.
Output: Updated `e2e/10-rentals-auth.spec.ts` with >=30 meaningful lines and clear assertions for both unauthorized and authorized rental route behavior.
</objective>

<execution_context>
@/Users/mspier/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rental-access-control-baseline/01-CONTEXT.md
@.planning/phases/01-rental-access-control-baseline/01-01-PLAN.md
@.planning/phases/01-rental-access-control-baseline/01-VERIFICATION.md
@e2e/10-rentals-auth.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand rentals auth e2e spec to satisfy failed artifact contract</name>
  <files>e2e/10-rentals-auth.spec.ts</files>
  <action>Refactor and expand the existing Playwright coverage so the spec has at least 30 meaningful lines and retains focused VIS-03 auth assertions. Keep assertions explicit for unauthenticated redirect to `/login` and authenticated access to `/rentals`. Add substantive checks (for example URL/path expectations before and after auth state transitions) rather than filler lines or comments.</action>
  <verify>
    <automated>test "$(wc -l < e2e/10-rentals-auth.spec.ts)" -ge 30</automated>
    <automated>rg -n "toHaveURL\\('/login'\\)|toHaveURL\\('/rentals\\?lang=en'\\)" e2e/10-rentals-auth.spec.ts</automated>
    <automated>npm run test:e2e -- e2e/10-rentals-auth.spec.ts</automated>
  </verify>
  <done>`e2e/10-rentals-auth.spec.ts` meets `min_lines >= 30`, contains meaningful auth assertions, and targeted e2e execution passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] test "$(wc -l < e2e/10-rentals-auth.spec.ts)" -ge 30
- [ ] npm run test:e2e -- e2e/10-rentals-auth.spec.ts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Verification gap in `01-VERIFICATION.md` is fully closed
- `e2e/10-rentals-auth.spec.ts` satisfies `min_lines >= 30` with meaningful assertions

</success_criteria>

<output>
After completion, create `.planning/phases/01-rental-access-control-baseline/01-02-SUMMARY.md`
</output>
