---
phase: 01-rental-access-control-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/rentals/page.tsx
  - components/dashboard/nav.tsx
  - messages/en.json
  - messages/pt-BR.json
  - lib/actions/rentals.ts
  - __tests__/lib/actions/rentals.test.ts
  - e2e/10-rentals-auth.spec.ts
autonomous: true
requirements:
  - VIS-03
user_setup: []
must_haves:
  truths:
    - Authenticated users can open `/rentals` from the dashboard and remain in protected app flow.
    - Unauthenticated visitors are blocked from rental pages and redirected to `/login` by existing auth behavior.
    - Rental server actions reject unauthenticated execution before validation or mutation logic runs.
    - Auth failures surface as explicit redirect/denied outcomes, never partial writes or silent success.
  artifacts:
    - path: app/(dashboard)/rentals/page.tsx
      provides: Rental baseline page under dashboard auth gate
      min_lines: 20
      contains: rentals
    - path: lib/actions/rentals.ts
      provides: Rental server action entrypoints with auth-first guard
      exports:
        - createRental
      contains: await requireAuth()
    - path: __tests__/lib/actions/rentals.test.ts
      provides: Unit coverage for unauthenticated block and authenticated pass-through
      min_lines: 30
    - path: e2e/10-rentals-auth.spec.ts
      provides: Browser-level verification for rental route auth behavior
      min_lines: 30
  key_links:
    - from: components/dashboard/nav.tsx
      to: app/(dashboard)/rentals/page.tsx
      via: Navigation link to rentals route
      pattern: /rentals
    - from: lib/actions/rentals.ts
      to: lib/auth/session.ts
      via: requireAuth call at each action entrypoint
      pattern: requireAuth\(\)
    - from: e2e/10-rentals-auth.spec.ts
      to: app/(dashboard)/rentals/page.tsx
      via: Playwright navigation and redirect assertions
      pattern: /rentals
---

<objective>
Implement the Phase 1 rental auth baseline by introducing a protected rentals route and rental action entrypoints that enforce authentication-only access.

Purpose: Satisfy VIS-03 for Phase 1 without adding RBAC or role differentiation.
Output: Rental route scaffold, auth-guarded rental server actions, and automated unit/e2e coverage for authorized and unauthorized flows.
</objective>

<execution_context>
@/Users/mspier/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-rental-access-control-baseline/01-CONTEXT.md
@.planning/phases/01-rental-access-control-baseline/01-RESEARCH.md
@CLAUDE.md
@app/(dashboard)/layout.tsx
@components/dashboard/nav.tsx
@lib/auth/session.ts
@lib/actions/transactions.ts
@__tests__/lib/actions/auth.test.ts
@e2e/01-auth.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add protected rentals route entrypoint in dashboard UI</name>
  <files>app/(dashboard)/rentals/page.tsx, components/dashboard/nav.tsx, messages/en.json, messages/pt-BR.json</files>
  <action>Create `app/(dashboard)/rentals/page.tsx` as the rental baseline landing page so it inherits dashboard-level `requireAuth()` from `app/(dashboard)/layout.tsx`. Add a rentals nav link in `components/dashboard/nav.tsx` and corresponding i18n keys in both locale files. Keep this auth-only: do not introduce role checks, permission enums, or access matrices.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Authenticated dashboard users can navigate to `/rentals`; unauthenticated access follows existing login redirect behavior; no RBAC primitives are added.</done>
</task>

<task type="auto">
  <name>Task 2: Create rental server action module with auth-first guardrails</name>
  <files>lib/actions/rentals.ts</files>
  <action>Add `lib/actions/rentals.ts` with baseline exported rental action entrypoint(s) for Phase 2+ reuse (for example `createRental`). In every exported action, call `await requireAuth()` as the first executable statement before validation, DB calls, or path revalidation. Reuse existing action response shape conventions and avoid any role branching (`user.role`, permission checks, policy helpers).</action>
  <verify>
    <automated>rg -n "await requireAuth\\(\\)" lib/actions/rentals.ts</automated>
  </verify>
  <done>Each rental action has auth at entry and cannot proceed unauthenticated; implementation remains auth-only and role-agnostic.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated VIS-03 coverage for rental route and rental actions</name>
  <files>__tests__/lib/actions/rentals.test.ts, e2e/10-rentals-auth.spec.ts</files>
  <action>Create unit tests for `lib/actions/rentals.ts` proving unauthenticated calls fail via `requireAuth()` redirect outcome and authenticated calls proceed to next branch. Add Playwright spec to verify unauthenticated visit to `/rentals` redirects to `/login` and authenticated user can load `/rentals` successfully. Keep assertions focused on auth behavior only; do not test role variants.</action>
  <verify>
    <automated>npm test -- --run __tests__/lib/actions/rentals.test.ts</automated>
    <automated>npm run test:e2e -- e2e/10-rentals-auth.spec.ts</automated>
  </verify>
  <done>Automated tests fail if rental auth protections regress, and they pass when VIS-03 baseline behavior is intact.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] npm test -- --run __tests__/lib/actions/rentals.test.ts
- [ ] npm run test:e2e -- e2e/10-rentals-auth.spec.ts
- [ ] Confirm no RBAC types, tables, or permission helpers were introduced in this phase
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- VIS-03 is satisfied with authentication-only protection for rental routes and rental server actions
- Unauthorized outcomes are explicit and consistent (redirect/denied), with no partial or silent behavior

</success_criteria>

<output>
After completion, create `.planning/phases/01-rental-access-control-baseline/01-01-SUMMARY.md`
</output>
