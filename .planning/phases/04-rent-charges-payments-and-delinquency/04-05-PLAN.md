---
phase: 04-rent-charges-payments-and-delinquency
plan: 05
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - app/(dashboard)/rentals/overdue/page.tsx
  - app/(dashboard)/rentals/leases/[id]/page.tsx
  - app/(dashboard)/rentals/tenants/[id]/page.tsx
  - app/(dashboard)/rentals/layout.tsx
autonomous: true
requirements:
  - RENT-03
  - RENT-04

must_haves:
  truths:
    - "User can view overdue balances page with tenant, unit, amount, oldest due date"
    - "Lease detail page shows current balance"
    - "Tenant detail page shows aggregate balance across leases"
    - "Navigation includes Charges, Payments, Overdue links"
  artifacts:
    - path: "app/(dashboard)/rentals/overdue/page.tsx"
      provides: "Overdue balances list"
      route: "/rentals/overdue"
    - path: "app/(dashboard)/rentals/leases/[id]/page.tsx"
      provides: "Lease detail with balance display"
      updates: "existing file"
    - path: "app/(dashboard)/rentals/tenants/[id]/page.tsx"
      provides: "Tenant detail with aggregate balance"
      updates: "existing file"
    - path: "app/(dashboard)/rentals/layout.tsx"
      provides: "Navigation with new links"
      updates: "existing file"
  key_links:
    - from: "app/(dashboard)/rentals/overdue/page.tsx"
      to: "lib/db/rentals-charges.ts"
      via: "import getOverdueBalances"
    - from: "app/(dashboard)/rentals/leases/[id]/page.tsx"
      to: "lib/db/rentals-charges.ts"
      via: "import getLeaseBalance"
---

<objective>
Create overdue page and integrate balance display into lease/tenant details and navigation.
</objective>

<execution_context>
@/Users/mspier/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
# Dependencies
@.planning/phases/04-rent-charges-payments-and-delinquency/04-01-SUMMARY.md
@.planning/phases/04-rent-charges-payments-and-delinquency/04-02-SUMMARY.md

# Existing files to update
@app/(dashboard)/rentals/leases/[id]/page.tsx
@app/(dashboard)/rentals/tenants/[id]/page.tsx
@app/(dashboard)/rentals/layout.tsx

# CONTEXT.md decisions
- Overdue page: /rentals/overdue
- Balance visible on lease detail page
- Tenant-level aggregate: total balance across all tenant's leases
- Grace period default: 5 days
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create overdue balances page</name>
  <files>app/(dashboard)/rentals/overdue/page.tsx</files>
  <action>
Create app/(dashboard)/rentals/overdue/page.tsx:

1. Fetch overdue balances via getOverdueBalances(5) with 5-day grace period
2. Display header: "Overdue Balances"
3. If no overdue: show "No overdue balances" message
4. If overdue exist: show table with columns:
   - Tenant Name
   - Unit (property + unit number)
   - Total Overdue Amount (formatCurrency)
   - Oldest Due Date
5. Show count summary: "X account(s) with overdue balance"

This implements RENT-04: View overdue balances
  </action>
  <verify>
<automated>test -f app/(dashboard)/rentals/overdue/page.tsx && grep -q "getOverdueBalances" app/(dashboard)/rentals/overdue/page.tsx</automated>
  </verify>
  <done>Overdue page accessible at /rentals/overdue</done>
</task>

<task type="auto">
  <name>Task 2: Add balance display to lease detail</name>
  <files>app/(dashboard)/rentals/leases/[id]/page.tsx</files>
  <action>
Update app/(dashboard)/rentals/leases/[id]/page.tsx:

1. Import getLeaseBalance from lib/db/rentals-charges.ts
2. In the page component, fetch balance: const balance = await getLeaseBalance(lease.id)
3. Add a new Card section after the lease info showing:
   - "Current Balance" label
   - Balance amount with formatCurrency
   - Color: red if balance > 0, green if 0 or negative
   - Text: "Amount due" or "Credit balance" based on value

This implements RENT-03: Balance viewable per lease
  </action>
  <verify>
<automated>grep -q "getLeaseBalance" app/(dashboard)/rentals/leases/[id]/page.tsx</automated>
  </verify>
  <done>Lease detail shows current balance</done>
</task>

<task type="auto">
  <name>Task 3: Add aggregate balance to tenant detail</name>
  <files>app/(dashboard)/rentals/tenants/[id]/page.tsx</files>
  <action>
Update app/(dashboard)/rentals/tenants/[id]/page.tsx:

1. Import getLeaseBalance from lib/db/rentals-charges.ts
2. Get all leases for this tenant
3. Calculate aggregate balance: sum of getLeaseBalance for each lease
4. Display total balance in the tenant info section
5. Optionally show per-lease breakdown if tenant has multiple leases

This implements RENT-03: Balance viewable per tenant (aggregate)
  </action>
  <verify>
<automated>grep -q "getLeaseBalance" app/(dashboard)/rentals/tenants/[id]/page.tsx</automated>
  </verify>
  <done>Tenant detail shows aggregate balance across leases</done>
</task>

<task type="auto">
  <name>Task 4: Add Charges, Payments, Overdue to navigation</name>
  <files>app/(dashboard)/rentals/layout.tsx</files>
  <action>
Update app/(dashboard)/rentals/layout.tsx navigation:

Add to the sidebar nav (following existing pattern):
- Charges: /rentals/charges
- Payments: /rentals/payments  
- Overdue: /rentals/overdue

Use appropriate icons from lucide-react (DollarSign, CreditCard, AlertTriangle or similar)
  </action>
  <verify>
<automated>grep -q "/rentals/charges" app/(dashboard)/rentals/layout.tsx && grep -q "/rentals/payments" app/(dashboard)/rentals/layout.tsx && grep -q "/rentals/overdue" app/(dashboard)/rentals/layout.tsx</automated>
  </verify>
  <done>Navigation includes Charges, Payments, Overdue links</done>
</task>

</tasks>

<verification>
[ ] Overdue page exists at /rentals/overdue
[ ] Lease detail shows balance
[ ] Tenant detail shows aggregate balance
[ ] Navigation has Charges, Payments, Overdue links
[ ] Grace period of 5 days applied to overdue query
</verification>

<success_criteria>
User can:
1. View all overdue balances on dedicated page
2. See balance on each lease detail
3. See aggregate balance on each tenant detail
4. Navigate to charges, payments, overdue from sidebar

This satisfies:
- RENT-03: User can see current balance due per lease and per tenant
- RENT-04: User can mark and view overdue balances based on due date and configurable grace period
</success_criteria>

<output>
After completion, create `.planning/phases/04-rent-charges-payments-and-delinquency/04-05-SUMMARY.md`
</output>
