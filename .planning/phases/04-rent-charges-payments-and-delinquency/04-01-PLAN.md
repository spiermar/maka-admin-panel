---
phase: 04-rent-charges-payments-and-delinquency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema.sql
  - lib/db/types.ts
  - lib/validations/rentals-charge.ts
  - lib/validations/rentals-payment.ts
autonomous: true
requirements:
  - RENT-01
  - RENT-02
  - RENT-03
  - RENT-04

must_haves:
  truths:
    - "Charges table exists with lease_id, charge_date, due_date, amount, status"
    - "Payments table exists with lease_id, payment_date, amount, payment_method"
    - "Types for RentCharge and RentPayment are exported from lib/db/types.ts"
    - "Zod validation schemas exist for creating charges and payments"
  artifacts:
    - path: "lib/db/schema.sql"
      provides: "charges and payments tables"
      contains: "CREATE TABLE charges", "CREATE TABLE payments"
    - path: "lib/db/types.ts"
      provides: "TypeScript types for charges and payments"
      exports: ["RentCharge", "RentPayment"]
    - path: "lib/validations/rentals-charge.ts"
      provides: "Zod schema for charge validation"
      exports: ["createChargeSchema"]
    - path: "lib/validations/rentals-payment.ts"
      provides: "Zod schema for payment validation"
      exports: ["createPaymentSchema"]
  key_links:
    - from: "lib/db/schema.sql"
      to: "lib/db/types.ts"
      via: "Types reference table columns"
    - from: "lib/actions/rentals.ts"
      to: "lib/validations/rentals-charge.ts"
      via: "import createChargeSchema"
---

<objective>
Create database schema, TypeScript types, and validation schemas for rent charges and payments.
</objective>

<execution_context>
@/Users/mspier/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-rent-charges-payments-and-delinquency/04-CONTEXT.md
@.planning/phases/04-rent-charges-payments-and-delinquency/04-RESEARCH.md

# Existing patterns from Phase 3
@lib/db/schema.sql (leases table structure, index patterns)
@lib/db/types.ts (Lease, Tenant types)
@lib/validations/rentals-lease.ts (Zod schema patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add charges and payments tables to schema.sql</name>
  <files>lib/db/schema.sql</files>
  <action>
Add two new tables after the leases table:
1. **charges table**: id, lease_id (FK), charge_date, due_date, amount (DECIMAL), status (pending/paid), created_at, updated_at
2. **payments table**: id, lease_id (FK), payment_date, amount, payment_method (cash/check/bank_transfer/other), notes, created_at, updated_at

Add indexes:
- idx_charges_lease_id on charges(lease_id)
- idx_charges_status on charges(status)
- idx_charges_due_date on charges(due_date)
- idx_payments_lease_id on payments(lease_id)
- idx_payments_date on payments(payment_date)

Follow existing table patterns (defaults, check constraints, etc.)
  </action>
  <verify>
<automated>grep -q "CREATE TABLE charges" lib/db/schema.sql && grep -q "CREATE TABLE payments" lib/db/schema.sql</automated>
  </verify>
  <done>charges and payments tables exist in schema.sql with proper columns and indexes</done>
</task>

<task type="auto">
  <name>Task 2: Add RentCharge and RentPayment types</name>
  <files>lib/db/types.ts</files>
  <action>
Add TypeScript interfaces after existing types:
```typescript
export interface RentCharge {
  id: number;
  lease_id: number;
  charge_date: string;
  due_date: string;
  amount: number;
  status: 'pending' | 'paid';
  created_at: Date;
  updated_at: Date;
}

export interface RentPayment {
  id: number;
  lease_id: number;
  payment_date: string;
  amount: number;
  payment_method: 'cash' | 'check' | 'bank_transfer' | 'other';
  notes: string | null;
  created_at: Date;
}
```
  </action>
  <verify>
<automated>grep -q "export interface RentCharge" lib/db/types.ts && grep -q "export interface RentPayment" lib/db/types.ts</automated>
  </verify>
  <done>RentCharge and RentPayment interfaces exported from types.ts</done>
</task>

<task type="auto">
  <name>Task 3: Create charge validation schema</name>
  <files>lib/validations/rentals-charge.ts</files>
  <action>
Create new validation file with Zod schema for charges:
- lease_id: required number
- charge_date: required, YYYY-MM-DD format
- due_date: required, YYYY-MM-DD format  
- amount: required, positive number
- status: optional, defaults to 'pending'

Follow patterns from lib/validations/rentals-lease.ts
  </action>
  <verify>
<automated>grep -q "createChargeSchema" lib/validations/rentals-charge.ts</automated>
  </done>
  <done>createChargeSchema exported and usable in server actions</done>
</task>

<task type="auto">
  <name>Task 4: Create payment validation schema</name>
  <files>lib/validations/rentals-payment.ts</files>
  <action>
Create new validation file with Zod schema for payments:
- lease_id: required number
- payment_date: required, YYYY-MM-DD format
- amount: required, positive number
- payment_method: required, enum (cash, check, bank_transfer, other)
- notes: optional string

Follow patterns from lib/validations/rentals-lease.ts
  </action>
  <verify>
<automated>grep -q "createPaymentSchema" lib/validations/rentals-payment.ts</automated>
  </done>
  <done>createPaymentSchema exported and usable in server actions</done>
</task>

</tasks>

<verification>
[ ] schema.sql has charges and payments tables
[ ] schema.sql has required indexes on new tables
[ ] types.ts exports RentCharge and RentPayment
[ ] rentals-charge.ts exports createChargeSchema
[ ] rentals-payment.ts exports createPaymentSchema
</verification>

<success_criteria>
All 4 requirements (RENT-01, RENT-02, RENT-03, RENT-04) will use these schema/types for:
- Storing charge and payment records
- Validating form inputs
- Type-safe DB operations

Database tables created via prisma db push or SQL after this plan executes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rent-charges-payments-and-delinquency/04-01-SUMMARY.md`
</output>