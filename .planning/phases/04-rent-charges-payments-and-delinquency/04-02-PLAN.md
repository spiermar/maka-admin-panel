---
phase: 04-rent-charges-payments-and-delinquency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/rentals-charges.ts
  - lib/db/rentals-payments.ts
autonomous: true
requirements:
  - RENT-01
  - RENT-02
  - RENT-03
  - RENT-04

must_haves:
  truths:
    - "generateMonthlyCharges() creates charges for all active leases in a month"
    - "createPayment() records payment and allocates to oldest pending charges"
    - "getLeaseBalance() calculates pending charges minus payments"
    - "getOverdueBalances() returns charges past due date + grace period"
  artifacts:
    - path: "lib/db/rentals-charges.ts"
      provides: "Charge CRUD + balance + overdue queries"
      exports: ["generateMonthlyCharges", "getAllCharges", "getLeaseBalance", "getOverdueBalances"]
    - path: "lib/db/rentals-payments.ts"
      provides: "Payment CRUD + allocation"
      exports: ["createPayment", "getAllPayments", "getPaymentsByLease"]
  key_links:
    - from: "lib/db/rentals-charges.ts"
      to: "lib/db/rentals-leases.ts"
      via: "getAllLeases({status: 'Active'}) for charge generation"
    - from: "lib/db/rentals-payments.ts"
      to: "lib/db/rentals-charges.ts"
      via: "update charge status after payment allocation"
---

<objective>
Implement database operations for charges, payments, balance calculation, and overdue queries.
</objective>

<execution_context>
@/Users/mspier/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
# From Plan 04-01
@lib/db/types.ts (RentCharge, RentPayment interfaces)
@lib/validations/rentals-charge.ts
@lib/validations/rentals-payment.ts

# Established patterns
@lib/db/rentals-leases.ts (query patterns, executeReturning, queryMany)
@lib/db/schema.sql

# CONTEXT.md decisions
- Payment allocation: oldest pending charge first
- Balance = pending charges - payments applied
- Grace period: default 5 days
- Partial payments allowed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rentals-charges.ts DB module</name>
  <files>lib/db/rentals-charges.ts</files>
  <action>
Create lib/db/rentals-charges.ts with:

1. **generateMonthlyCharges(year, month, gracePeriodDays = 5)**
   - Get all Active leases where start_date <= month_end AND end_date >= month_start
   - Create one charge per lease for that month
   - Calculate due_date = charge_date + grace period days
   - Skip if charge already exists for that lease/month (check on lease_id + charge_date)
   - Return created charges array

2. **getChargeById(id)** - single charge or null

3. **getAllCharges(filters?: { status?, month? })** - list with optional filters

4. **getLeaseBalance(leaseId)** - sum of pending charges minus payments for lease

5. **getOverdueBalances(gracePeriodDays = 5)** - returns array with lease_id, tenant_name, unit_info, total_overdue, oldest_due_date for all overdue charges

Use executeReturning for inserts, queryOne/queryMany for selects. Follow patterns from rentals-leases.ts
  </action>
  <verify>
<automated>grep -q "generateMonthlyCharges" lib/db/rentals-charges.ts && grep -q "getLeaseBalance" lib/db/rentals-charges.ts</automated>
  </done>
  <done>Charges module exports all required functions for RENT-01, RENT-03, RENT-04</done>
</task>

<task type="auto">
  <name>Task 2: Create rentals-payments.ts DB module</name>
  <files>lib/db/rentals-payments.ts</files>
  <action>
Create lib/db/rentals-payments.ts with:

1. **createPayment(data)** - insert payment record
   - Create payment with provided data

2. **getPaymentById(id)** - single payment or null

3. **getAllPayments(filters?: { lease_id?, start_date?, end_date? })** - list with filters

4. **getPaymentsByLease(leaseId)** - all payments for a lease, ordered by payment_date DESC

5. **allocatePaymentToCharges(paymentId)** - INTERNAL
   - Find pending charges for lease ordered by due_date ASC (oldest first)
   - Apply payment amount to charges in order until exhausted
   - Update charge status: paid if fully covered, pending if partial

This implements the "oldest pending charge first" allocation per CONTEXT.md
  </action>
  <verify>
<automated>grep -q "createPayment" lib/db/rentals-payments.ts && grep -q "allocatePaymentToCharges" lib/db/rentals-payments.ts</automated>
  </done>
  <done>Payments module exports all required functions for RENT-02</done>
</task>

</tasks>

<verification>
[ ] rentals-charges.ts exists and exports generateMonthlyCharges, getAllCharges, getLeaseBalance, getOverdueBalances
[ ] rentals-payments.ts exists and exports createPayment, getAllPayments, getPaymentsByLease
[ ] generateMonthlyCharges checks for existing charges before creating
[ ] getLeaseBalance calculates pending charges - payments
[ ] allocatePaymentToCharges orders by due_date ASC
</verification>

<success_criteria>
DB modules provide all data operations needed by UI and server actions:
- Charge generation from active leases
- Payment recording with auto-allocation
- Balance calculation per lease
- Overdue balance detection
</success_criteria>

<output>
After completion, create `.planning/phases/04-rent-charges-payments-and-delinquency/04-02-SUMMARY.md`
</output>