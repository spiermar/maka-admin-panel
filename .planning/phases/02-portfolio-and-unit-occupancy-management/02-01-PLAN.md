---
phase: 02-portfolio-and-unit-occupancy-management
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/migrations/003_create_rentals_inventory.sql
  - lib/db/schema.sql
  - lib/db/types.ts
  - lib/db/rentals-properties.ts
  - lib/db/rentals-units.ts
  - lib/validations/rentals-property.ts
  - lib/validations/rentals-unit.ts
  - lib/actions/rentals.ts
  - __tests__/lib/db/rentals-properties.test.ts
  - __tests__/lib/db/rentals-units.test.ts
  - __tests__/lib/validations/rentals-unit.test.ts
autonomous: true
requirements:
  - UNIT-01
user_setup: []
must_haves:
  truths:
    - Users can create and update properties needed to organize residential units.
    - Users can create and update units under a property using required residential attributes only.
    - Unit numbers are unique within a property and cannot collide within the same property.
    - No building entity is introduced; optional building context is stored as unit-level `building_label` only.
  artifacts:
    - path: lib/db/migrations/003_create_rentals_inventory.sql
      provides: Properties and units schema with property-scoped unit uniqueness
      contains: UNIQUE (property_id, unit_number)
    - path: lib/db/rentals-units.ts
      provides: Unit read/write queries bound to Property -> Unit hierarchy
      min_lines: 40
    - path: lib/validations/rentals-unit.ts
      provides: Required unit creation/update validation for core residential fields
      contains: building_label
    - path: lib/actions/rentals.ts
      provides: Auth-first server actions for property and unit management
      contains: await requireAuth()
  key_links:
    - from: lib/actions/rentals.ts
      to: lib/validations/rentals-unit.ts
      via: Server action validation before DB writes
      pattern: safeParse
    - from: lib/actions/rentals.ts
      to: lib/db/rentals-units.ts
      via: Action invokes unit query layer for persistence
      pattern: rentals-units
    - from: lib/db/rentals-units.ts
      to: lib/db/migrations/003_create_rentals_inventory.sql
      via: Query logic relies on property-scoped uniqueness constraint
      pattern: property_id
---

<objective>
Implement the Phase 2 UNIT-01 foundation by introducing property and unit inventory persistence, validation, and auth-protected mutation entrypoints.

Purpose: Establish the canonical `Property -> Unit` model and required unit attributes so occupancy and inventory flows can build on stable data contracts.
Output: Rental inventory schema/migration, db query modules, zod validations, rental actions, and focused unit tests for property/unit management rules.
</objective>

<execution_context>
@/Users/mspier/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-portfolio-and-unit-occupancy-management/02-CONTEXT.md
@.planning/phases/02-portfolio-and-unit-occupancy-management/02-RESEARCH.md
@.planning/phases/02-portfolio-and-unit-occupancy-management/02-VALIDATION.md
@CLAUDE.md
@lib/db/schema.sql
@lib/db/types.ts
@lib/actions/accounts.ts
@lib/actions/transactions.ts
@lib/validations/transactions.ts
@__tests__/lib/db/accounts.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Property -> Unit schema and query modules for UNIT-01</name>
  <files>lib/db/migrations/003_create_rentals_inventory.sql, lib/db/schema.sql, lib/db/types.ts, lib/db/rentals-properties.ts, lib/db/rentals-units.ts</files>
  <action>Add properties and units tables with canonical `Property -> Unit` hierarchy and enforce `unit_number` uniqueness per property. Include optional `building_label` on units, but do not add any Building table/entity. Keep unit status field aligned with Phase 2 status set (`Occupied`, `Vacant`, `Unavailable`) to support required unit attributes while deferring effective-date scheduling to the occupancy plan. Export corresponding DB types and implement query modules for create/update/read/list behavior.</action>
  <verify>
    <automated>npm test -- --run __tests__/lib/db/rentals-properties.test.ts __tests__/lib/db/rentals-units.test.ts</automated>
  </verify>
  <done>Schema and db modules support property/unit CRUD with property-scoped unit uniqueness and optional unit-level `building_label`, with no standalone Building model introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Add UNIT-01 validation contracts for required residential unit attributes</name>
  <files>lib/validations/rentals-property.ts, lib/validations/rentals-unit.ts, __tests__/lib/validations/rentals-unit.test.ts</files>
  <action>Create zod schemas for property and unit create/update payloads. Enforce required core unit fields exactly: property, unit number, unit type, bedroom count, bathroom count, and status. Allow optional `building_label`. Keep field coercion and error shape consistent with existing action-validation conventions.</action>
  <verify>
    <automated>npm test -- --run __tests__/lib/validations/rentals-unit.test.ts</automated>
  </verify>
  <done>Validation fails for missing required core fields and accepts optional `building_label` without introducing out-of-scope fields.</done>
</task>

<task type="auto">
  <name>Task 3: Wire auth-first rental property/unit server actions</name>
  <files>lib/actions/rentals.ts</files>
  <action>Implement property/unit create and update server actions in `lib/actions/rentals.ts` that call `await requireAuth()` first, then validate with rentals schemas, then persist through rentals db modules, then `revalidatePath` for rentals routes. Keep action outcomes explicit and deterministic; do not add modal-only flows or non-phase abstractions.</action>
  <verify>
    <automated>rg -n "await requireAuth\(\)" lib/actions/rentals.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>All property/unit mutation entrypoints are auth-first and aligned to existing action architecture, with revalidation hooks for downstream inventory pages.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] npm test -- --run __tests__/lib/db/rentals-properties.test.ts __tests__/lib/db/rentals-units.test.ts __tests__/lib/validations/rentals-unit.test.ts
- [ ] Confirm unit uniqueness is constrained by `(property_id, unit_number)` only
- [ ] Confirm no standalone Building entity/table was added
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- UNIT-01 is satisfied for property/unit management with required residential attributes
- Canonical inventory model remains `Property -> Unit` with optional unit-level `building_label`

</success_criteria>

<output>
After completion, create `.planning/phases/02-portfolio-and-unit-occupancy-management/02-01-SUMMARY.md`
</output>
