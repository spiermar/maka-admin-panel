---
phase: 02-portfolio-and-unit-occupancy-management
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - lib/db/migrations/004_create_unit_occupancy_statuses.sql
  - lib/db/schema.sql
  - lib/db/types.ts
  - lib/db/rentals-occupancy.ts
  - lib/validations/rentals-occupancy.ts
  - lib/actions/rentals.ts
  - __tests__/lib/db/rentals-occupancy.test.ts
  - __tests__/lib/validations/rentals-occupancy.test.ts
  - __tests__/lib/actions/rentals-occupancy.test.ts
autonomous: true
requirements:
  - UNIT-02
user_setup: []
must_haves:
  truths:
    - Users can set unit status to `Occupied`, `Vacant`, or `Unavailable` using effective dates.
    - Status changes support both current-date and future-date scheduling using date-only semantics.
    - Overlapping scheduled status entries for the same unit are blocked and require explicit user resolution.
    - Current status (effective today) and next future status can be derived reliably from occupancy history.
  artifacts:
    - path: lib/db/migrations/004_create_unit_occupancy_statuses.sql
      provides: Effective-date occupancy history table and overlap-safe constraints
      contains: effective_date
    - path: lib/db/rentals-occupancy.ts
      provides: Occupancy scheduling and current/future status query helpers
      min_lines: 50
    - path: lib/validations/rentals-occupancy.ts
      provides: Status enum and date-only validation rules
      contains: Occupied
    - path: __tests__/lib/actions/rentals-occupancy.test.ts
      provides: Action-level tests for overlap rejection and scheduling paths
      min_lines: 30
  key_links:
    - from: lib/actions/rentals.ts
      to: lib/validations/rentals-occupancy.ts
      via: Action schema validation of status and effective date input
      pattern: rentals-occupancy
    - from: lib/actions/rentals.ts
      to: lib/db/rentals-occupancy.ts
      via: Authenticated occupancy scheduling writes and reads
      pattern: occupancy
    - from: lib/db/rentals-occupancy.ts
      to: lib/db/migrations/004_create_unit_occupancy_statuses.sql
      via: Query semantics rely on date-only effective history structure
      pattern: effective_date
---

<objective>
Implement Phase 2 UNIT-02 by adding effective-date occupancy history and scheduling behavior for units with strict date/status constraints.

Purpose: Make occupancy state trustworthy for operational decisions by preventing schedule conflicts and preserving date-based status transitions.
Output: Occupancy schema layer, query/validation/action support for status scheduling, and automated tests covering date rules and overlap prevention.
</objective>

<execution_context>
@/Users/mspier/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-portfolio-and-unit-occupancy-management/02-CONTEXT.md
@.planning/phases/02-portfolio-and-unit-occupancy-management/02-RESEARCH.md
@.planning/phases/02-portfolio-and-unit-occupancy-management/02-VALIDATION.md
@.planning/phases/02-portfolio-and-unit-occupancy-management/02-01-PLAN.md
@CLAUDE.md
@lib/actions/rentals.ts
@lib/db/schema.sql
@lib/db/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add occupancy history schema and date-effective query primitives</name>
  <files>lib/db/migrations/004_create_unit_occupancy_statuses.sql, lib/db/schema.sql, lib/db/types.ts, lib/db/rentals-occupancy.ts</files>
  <action>Create `unit_occupancy_statuses` persistence for date-effective transitions tied to units. Restrict statuses to `Occupied`, `Vacant`, `Unavailable`, store effective dates as SQL `DATE` (not timestamp), and implement query helpers that resolve current status (`effective_date <= CURRENT_DATE`) plus next scheduled future status. Ensure overlap/conflict cases are detected and rejected so conflicting schedules cannot be committed.</action>
  <verify>
    <automated>npm test -- --run __tests__/lib/db/rentals-occupancy.test.ts</automated>
  </verify>
  <done>Occupancy history supports current/future resolution with date-only behavior, and conflicting scheduled rows for a unit are blocked.</done>
</task>

<task type="auto">
  <name>Task 2: Implement occupancy validation rules for status/date semantics</name>
  <files>lib/validations/rentals-occupancy.ts, __tests__/lib/validations/rentals-occupancy.test.ts</files>
  <action>Define zod schemas for occupancy status scheduling payloads. Enforce allowed statuses only, enforce `YYYY-MM-DD` date-only input semantics, allow current and future effective dates, and allow optional unavailable reason only when status is `Unavailable`. Keep validation messages/action-error shape compatible with existing server action patterns.</action>
  <verify>
    <automated>npm test -- --run __tests__/lib/validations/rentals-occupancy.test.ts</automated>
  </verify>
  <done>Validation accepts valid date-only occupancy transitions and rejects invalid status/date combinations or malformed payloads.</done>
</task>

<task type="auto">
  <name>Task 3: Add auth-first occupancy actions and overlap rejection tests</name>
  <files>lib/actions/rentals.ts, __tests__/lib/actions/rentals-occupancy.test.ts</files>
  <action>Extend `lib/actions/rentals.ts` with auth-first occupancy scheduling actions that validate input, call occupancy query module, block overlap conflicts with explicit error outcomes, and revalidate affected rentals pages. Add action tests for unauthenticated blocking, accepted current/future effective dates, and explicit rejection of overlapping scheduled status updates.</action>
  <verify>
    <automated>npm test -- --run __tests__/lib/actions/rentals-occupancy.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Occupancy actions enforce date/status rules and reject overlaps with explicit responses, with regression tests guarding behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] npm test -- --run __tests__/lib/db/rentals-occupancy.test.ts __tests__/lib/validations/rentals-occupancy.test.ts __tests__/lib/actions/rentals-occupancy.test.ts
- [ ] Confirm only `Occupied`, `Vacant`, and `Unavailable` statuses are accepted
- [ ] Confirm all effective-date comparisons are date-only (no timestamp/timezone logic)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- UNIT-02 is satisfied with effective-date occupancy scheduling and overlap prevention
- Occupancy behavior supports current + future context without introducing out-of-scope bulk updates

</success_criteria>

<output>
After completion, create `.planning/phases/02-portfolio-and-unit-occupancy-management/02-02-SUMMARY.md`
</output>
