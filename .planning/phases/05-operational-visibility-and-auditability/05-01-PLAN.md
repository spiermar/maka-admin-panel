---
phase: 05-operational-visibility-and-auditability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/analytics/rentals-operations.ts
  - components/dashboard/summary-cards.tsx
autonomous: true
requirements:
  - VIS-01

must_haves:
  truths:
    - Dashboard shows current vacant unit count
    - Dashboard shows current occupied unit count
    - Dashboard shows delinquent account count
    - Click-through from cards goes to relevant filtered pages
  artifacts:
    - path: "lib/analytics/rentals-operations.ts"
      provides: "Rental operation summary queries"
      exports: ["getRentalOperationSummary"]
    - path: "components/dashboard/summary-cards.tsx"
      provides: "Extended with rental summary cards"
      contains: "RentalOperationSummary"
  key_links:
    - from: "components/dashboard/summary-cards.tsx"
      to: "lib/analytics/rentals-operations.ts"
      via: "import getRentalOperationSummary"
      pattern: "import.*getRentalOperationSummary"
---

<objective>
Implement dashboard summary cards for rental operations visibility (VIS-01).

Purpose: Provide landlords with immediate visibility into vacancy, occupied units, and delinquent accounts without navigating to individual pages.
Output: Extended dashboard with rental operation summary cards.
</objective>

<execution_context>
@/Users/mspier/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/(dashboard)/page.tsx
@components/dashboard/summary-cards.tsx
@lib/analytics/cash-flow.ts
@lib/db/rentals-units.ts

# Reference patterns from existing analytics
@lib/db/schema.sql (units table structure - status column)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rental operations analytics queries</name>
  <files>lib/analytics/rentals-operations.ts</files>
  <action>
Create new file lib/analytics/rentals-operations.ts with:

1. Export interface RentalOperationSummary with:
   - vacant_count: number
   - occupied_count: number
   - unavailable_count: number
   - delinquent_count: number

2. Export async function getRentalOperationSummary() that:
   - Queries units table to get vacant/occupied/unavailable counts using status column
   - Queries charges table for delinquent accounts (pending status where due_date < today - 5 day grace period)
   - Returns RentalOperationSummary object

Use the existing queryOne pattern from @lib/db (import from '@/lib/db').
Reference schema.sql for exact column names (status in units table, status/due_date in charges table).
  </action>
  <verify>
    <automated>npm run lint && npm run build 2>&1 | head -30</automated>
  </verify>
  <done>lib/analytics/rentals-operations.ts exports getRentalOperationSummary function that returns rental operation counts</done>
</task>

<task type="auto">
  <name>Task 2: Add rental summary cards to dashboard</name>
  <files>components/dashboard/summary-cards.tsx</files>
  <action>
Extend the existing SummaryCards component in components/dashboard/summary-cards.tsx:

1. Import getRentalOperationSummary from '@/lib/analytics/rentals-operations'

2. Add a new section after the existing CashFlow cards (after line 77) with:
   - Vacant Units card - links to /rentals/units?status=Vacant
   - Occupied Units card - links to /rentals/units?status=Occupied  
   - Delinquent Accounts card - links to /rentals/overdue (red styling)

3. Use the same Card, CardHeader, CardTitle, CardContent pattern as existing cards.

4. Render these cards in a new grid row below the existing financial summary cards.

Note: Do NOT modify the existing cards - only add new rental summary cards.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard shows three new summary cards for rental operations with click-through links</done>
</task>

</tasks>

<verification>
- [ ] Dashboard page loads without errors
- [ ] New rental summary cards render with counts
- [ ] Cards link to appropriate filtered pages
- [ ] Lint and type check pass
</verification>

<success_criteria>
Dashboard displays vacancy count, occupied count, and delinquent account count. Clicking each card navigates to the relevant filtered view.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-visibility-and-auditability/05-01-SUMMARY.md`
</output>